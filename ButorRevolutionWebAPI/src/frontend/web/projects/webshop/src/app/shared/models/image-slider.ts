export interface ConfigImageSlider {
    slidesToShow?: number;
    slidesToScroll?: number;
    infinite?: boolean;
}

export interface IimagesSliderModel {
    img: string;
    name?: string;
}

export interface IPictureModel {
    containerName?: string;
    fileName?: string;
}
