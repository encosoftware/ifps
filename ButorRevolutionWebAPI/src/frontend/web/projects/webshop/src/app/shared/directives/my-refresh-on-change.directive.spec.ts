import { MyRefreshOnChangeDirective } from './my-refresh-on-change.directive';

describe('MyRefreshOnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRefreshOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
